{{#exists}}
  <form class="form-horizontal" action="{{ base_url() }}/v1/posts/{{ comment.post_id }}/{{ id }}" method="post">
    <input type="hidden" name="_method" value="PUT" />
{{/exists}}
{{^exists}}
  <form class="form-horizontal" action="{{ base_url() }}/v1/posts/{{ comment.post_id }}" method="post">
{{/exists}}
 
 
    <div class="form-group">
      <label class="control-label">Author Name</label>
      <input class="form-control" type="text" name="author_name" value="{{ comment.author_name }}" />
    </div>
 
    <div class="form-group">
      <label class="control-label">Comment</label>
      <textarea name="content" class="form-control">{{ comment.content }}</textarea>
    </div>
 
    <div class="form-actions">
      <input type="submit" class="btn btn-primary" value="Save" />
    </div>
 
</form>